<?xml version="1.0" encoding="utf-8"?>
<!-- Backup and device transfer on Android 12+: only the settings DataStore (palette, mode, group size, timeout). -->
<data-extraction-rules>
    <cloud-backup>
        <include domain="file" path="datastore/settings.preferences_pb" />
    </cloud-backup>
    <device-transfer>
        <include domain="file" path="datastore/settings.preferences_pb" />
    </device-transfer>
</data-extraction-rules>
